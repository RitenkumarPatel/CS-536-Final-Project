\documentclass{article}

\usepackage{hyperref}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage[normalem]{ulem}
\usepackage{graphicx}
\usepackage{float}

\graphicspath{{figures}}

\title{CS536 Final Project}
\author{Author}
\date{}

\begin{document}
\maketitle

\section{Motivation}
Throughout this course, we have mostly considered learning settings where there is a single entity handling training and testing.
However, these techniques are not always applicable to scenarios like edge computing and IoT devices.
Often, there may be multiple devices with disjoint data sets that need to be aggregated to train a shared model.
These devices may also have bandwidth or privacy requirements, preventing users from directly aggregating data then training.
This is addressed by techniques in federated learning \cite{yang2019}.

There are several ways data can be split among multiple devices.
In the first case, each device can hold a subset of the data, recording different events with identical features.
This is the problem that \underline{horizontal federated learning} solves \cite{li2020}.
Alternatively, perhaps each device records some information about the same event, but they hold a disjoint set of features.
We address this problem with \underline{vertical federated learning} \cite{feng2020}.

\section{Horizontal Federated Learning}
Let's begin by formalizing the horizontal federated learning problem. Suppose we have $n$ different sensors. Our data will be $\mathbf{x} \in \mathbb{R}^{k \times m}$ where $k$ is the number of distinct data points and $m$ is the number of features associated with a labels $y \in \mathbb{R}^k$. Our goal is to either find a global model $f: \mathbb{R}^{m} \to \mathbb{R}$ that predicts the label $y$ given $\mathbf{x} \in \mathbb{R}^m$ or, to have access to local models that can do the same. A 'global' model is constant across sensors while a 'local' model applies to a specific sensor. The data may not be IID, and so it is possible that each sensor is tasked with discovering a unique underlying distribution.

The task is called 'horizontal' because each sensor will be given a subset of the $k$ data points. For our purposes, we will be assuming each sensor gets the same number of data points to train on. 

\subsection{Data}
There are two distinct data types we explore: IID and heterogeneous. For the IID case, we generate $n$ samples based off $events$ number of features. Only $output\_ arity$ number of features influence the label, the rest are noise. The input features $\mathbf{x}$ are uniformly distributed between $-10$ and $10$. The label $y$ is generated by choosing a random subset of $output\_arity$ features and taking a linear combination of them with their weights being determined according to $\mathcal N (0, 1)$. This is then split across the sensors equally.

For the heterogeneous data, we generate the input features in the same way. For the labels, we again choose a subset of features. We then generate $base\_weight$ for each feature according to a standard normal distribution. Then, for each sensor, we have a $local\_weight \sim \mathcal N (base\_weight, heterogeneity\_std)$. This means each sensor has its own parameters for its underlying distribution.

\subsection{Algorithms}

We will explore three algorithms for federated learning explored in \cite{chen2024}. FedAvg works to construct a global model and is thus applicable to IID data. 
Our implementation of FedAvg:
\begin{enumerate}
    \item We initialize weights and intercepts for a linear regression model.
    \item The central server will broadcast the global model.
    \item Each sensor runs stochastic gradient descent and returns the new local weights.
    \item Take the mean of all the local weights, this becomes the new global model.
    \item repeat steps 2 - 5 for some number of iterations.
\end{enumerate}

Model Agnostic Meta Learning (MAML) \cite{finn2017} aims to find a global model that can easily be adjusted (via gradient descent) to a local learning task. The way to evaluate this algorithm is given the global model, run some number of local update steps with local validation data. Then, evaluate the MSE of that model against testing data.
Our implementation of MAML:
\begin{enumerate}
    \item Initialize weights and intercepts for a linear regression model. (In our code, you will see a single layer NN)
    \item Broadcast the global model weights $\theta$.
    \item Each sensor runs stochastic gradient descent and gets new hypothetical weights $\theta'$ which are a function of $\theta$.
    \item Take the gradient of the sum of the mean squared error across all sensors with respect to $\theta$ and update the global model with a gradient descent step.
    \item repeat steps 2 - 4 for some number of iterations.
\end{enumerate}
Note that we are taking the gradient of a gradient here to find the optimal meta parameter $\theta$.

Federated Multi Task Learning (FTML) aims to find a unique model for each sensor with the assumption that they are working on different but related learning tasks. We implemented the MOCHA algorithm \cite{smith2018} 

\begin{enumerate}
    \item Initialize a weight matrix $W \in \mathbb{R}^{d \times m}$ and a task relationship matrix $\Sigma$.
    \item \textbf{Inner Loop:} Each sensor runs Stochastic Dual Coordinate Ascent (SDCA) on its local data to find updates for its specific weights $W_t$.
    \item Sensors communicate these local updates back to the central server to update the shared weight matrix $W$.
    \item \textbf{Outer Loop:} Based on the updated $W$, the server updates the task relationship matrix $\Sigma$. 
    \item Repeat steps 2--4 for some number of iterations to refine both individual models and the global task relationships.
\end{enumerate}

\subsection{Questions of Interest}
\begin{enumerate}
    \item FedAvg generates one global model, so we expect it to perform poorly on IID data. 
    \item FMTL and MAML are different strategies for tackling heterogeneous distributed data, under what conditions does FTML outperform MAML and vice versa?
    \item How does the training dataset size affect performance?
    \item How does the numberx of sensors affect performance?
    \item How does the output arity affect performance?
\end{enumerate}

\subsection{FedAVG Results}

The base configuration for following plots are generated from 1000 data points, 100 features, an output ariity of 50, and 10 sensors.

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{FedAvg_datasetsize_homogeneous.png}
    \caption{Test MSE for FedAvg on homogeneous data versus dataset size. We see that more training data leads to higher accuracy.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{FedAvg_datasetsize_heterogeneous.png}
    \caption{Test MSE for FedAvg on heterogeneous data versus dataset size. Notice that the test MSE is significantly higher than in the homogeneous case, in line with our hypothesis. After about 200 datapoints, the test MSE stabilizes. This is likely becuase FedAvg can
    not capture the underlying distribution since the data is non-IID.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{FedAvg_arity_homogeneous.png}
    \caption{Test MSE for FedAvg on homogeneous data versus output arity. The test MSE increases as the number of significant features increases. This is likely because the regularization in our FedAvg algorithm is able to clear the noisy features. Therefore
    the function with less features is easier to learn.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{FedAvg_sensors_homogeneous.png}
    \caption{Test MSE for FedAvg on homogeneous data versus number of sensors. We see that splitting the data between more sensors makes the problem more difficult.}
\end{figure}

\subsection{MAML Results}

The base config for the following plots is a dataset size of 1000, 200 features, output arity of 50, and 10 sensors.

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{MAML_datasetsize_homogeneous.png}
    \caption{Test MSE for MAML on homogeneous data versus dataset size. More training data leads to higher accuracy.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{MAML_datasetsize_heterogeneous.png}
    \caption{Test MSE for MAML on heterogeneous data versus dataset size. The performance for heterogeneous data is very similar to homogeneous data.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=200px]{MAML_arity_homoogeneous.png}
    \caption{Test MSE for MAML on homogeneous data versus arity. Higher arity leads to better performance, which is opposite to the relationship we saw with FedAvg. This is probably because more features impacting the label 
    provides the model with more opportunity to see through the noise. }
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=200px]{MAML_arity_heterogeneous.png}
    \caption{Test MSE for MAML on heterogeneous data versus arity. This has the same relationship as the previous graph.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{MAML_sensors_homoogeneous.png}
    \caption{Test MSE for MAML on homogeneous data versus the number of sensors. Similar to FedAvg, more sensors leads to worse performance because the same amount of data is shared between more sensors.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{MAML_sensors_heterogeneous.png}
    \caption{Test MSE for MAML on heterogeneous data versus the number of sensors. This has the same relationship as the previous graph, but this has much less variability.}
\end{figure}

\subsection{FMTL Results}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{FTML_datasetsize_homogeneous.png}
    \caption{Test MSE for FMTL on homogeneous data versus the dataset size.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{FTML_datasetsize_heterogeneous.png}
    \caption{Test MSE for FMTL on heterogeneous data versus the dataset size.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{FMTL_arity_homogeneous.png}
    \caption{Test MSE for FMTL on homogeneous data against output arity. There is no significant relationship between output arity and test MSE.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{FMTL_arity_heterogeneous.png}
    \caption{Test MSE for FMTL on heterogeneous data against output arity. This, like previous graph, has no significant relationship.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{FMTL_sensors_homogeneous.png}
    \caption{Test MSE for FMTL on homogeneous data against number of sensors. A larger number of sensors leads to better performance. This is the only algorithm that has exhibited this relationship. This is because the MOCHA algorithm trains an entirely different model for each sensor. MAML tries to 
    generate a 'generic' model that gets weaker as more sensors create a more diverse underlying distribution.}
\end{figure}

\begin{figure}[H]
    \center
    \includegraphics[width=250px]{FMTL_sensors_heterogeneous.png}
    \caption{Test MSE for FMTL on heterogeneous data. This has the same relationship as the previous graph.}
\end{figure}


\section{Vertical Federated Learning}
In a general learning problem, we have $\mathbf x\in\mathbb R^m$ as an input vector and we want to learn some function $f:\mathbb R^m\to\mathbb R$ and predict the value $y=f(\mathbf x)$.
Consider the $k$ party case, where we have $m=\sum_{i=1}^km_i$, and $\mathbf x=\bigoplus_{i=1}^m\mathbf x_i\in\bigoplus_{i=1}^m\mathbb R^{m_i}$.
Party $i$ can only see $\mathbf x_i,y$, and needs to cooperate with other parties to predict $f(\mathbf x)$ without divulging their own data.
We implement a simplified version of \cite{wan2007}, as there is a data leakage attack which the extra random vector in said paper's implementation does not directly address.

For simplicity, consider the case of linear regression.
Take $f(\mathbf x)=\mathbf x\cdot\mathbf w=\sum_{i=1}^k\mathbf x_i\cdot\mathbf w_i$ for $\mathbf w\in\mathbb R^m$, where $\mathbf w=\sum_{i=1}^k\mathbf w_i\in\bigoplus_{i=1}^m\mathbb R^{m_i}$ and party $i$ only sees $\mathbf w_i$.
Given some $\mathbf x_i$, $y$, party $i$ will compute and broadcast $\mathbf x_i\cdot\mathbf w_i$, then collect $\hat y=\sum_{i=1}^k\mathbf x_i\cdot\mathbf w_i$ from other parties.
The goal is to minimize the squared error $\mathcal L=(y-\hat y)^2$.

To train this model, some data $\mathbf X=(\mathbf x^1,\ldots,\mathbf x^N)^\intercal\in\mathbb R^{N\times m}$ and output $\mathbf y(y_1,\ldots,y_N)^\intercal\in\mathbb R^N$ is partitioned to $\mathbf X_i\in\mathbb R^{N\times m_1}$.
Party $i$ obtains $\mathbf X_i$, $\mathbf y$, and initializes $\mathbf w_i$ to a random vector.
Then we run several rounds of gradient descent, adjusting $w_l$ for each feature $l\in[m]$ as follows.
\begin{align}
    \frac{\partial\mathcal L}{\partial w_l}=&
    \frac{\partial}{\partial w_l}\sum_{i=1}^N\left(\sum_{j=1}^m \mathbf x_i(j)\cdot w_j-y_i\right)^2\\
    =&\sum_{i=1}^N
    2\mathbf x_i(l)\left(\sum_{j=1}^m \mathbf x_i(j)\cdot w_j-y_i\right)\\
    =&\sum_{i=1}^N2\mathbf x_i(l)(\mathbf x_i\cdot\mathbf w-y_i)
\end{align}

The key insight here is that the $\mathbf x_i\cdot\mathbf w-y_i$ can be computed without view of each individual feature, thus updating $w_l$ only requires $\mathbf x_i(l)$.
The party that needs to update $w_l$ also owns $\mathbf x_i(l)$, so federated gradient descent is possible.

\subsection{Experiments}
We randomly generate 1000 data points with $\mathbf x\in\mathbb R^{100}$ and split the features among 5 parties (each holding 20 entries of $\mathbf x$ per case).
The function we want to predict is of the form $f(\mathbf x)=\mathbf w\cdot\mathbf x+\mathcal N(0,0.01)$, where $\mathbf w$ is nonzero for half of it's entries (randomly pick half of the features to matter, and other half to be irrelevant).
For now, we take each coordinate of $\mathbf x$ to be i.i.d. Gaussian.
We compare federated gradient descent with learning rate $\eta=1/10000$ and $T=300000$ (iterations) against Ridge regression with $\alpha=0.5$.
\begin{verbatim}
>>> Ridge regression
Train Error: 8.745205646168433e-05
Test Error: 0.00011756193048215462
>>> Federated gradient descent
Train 9.016817380303679e-05
Test 0.0001197005474922649
\end{verbatim}

This is not very surprising, since the above method is functionally equivalent to linear regression with gradient descent.
One natural question to ask is \uline{do we need to broadcast weights every iteration}?
Here, we have each party only broadcast an updated $\mathbf w\cdot\mathbf x$ inner product every $k$ rounds.

\begin{figure}[h]
    \center
    \includegraphics[width=200px]{fgd_delay}
    \caption{Graph of test and train MSE against gradient update frequency. Each party shares updated $\mathbf w\cdot\mathbf x$ every $k$ rounds. Note that model diverges for $k\ge 4$.}
\end{figure}

Looking at this graph, we observe that updating the gradient less often seems to perform better.
We note that locally at a point, the gradient does not change much, so this can be seen as some approximation of a larger learning rate.
Rerunning with $\eta=1/5000$ (doubling the learning rate), only converges for $k=1$, but increases loss by an order of 10.

\subsection{Gradient Inversion}
Earlier, we claimed that this method allowed data to be hidden from other parties.
However, this is not exactly true.
Note that at each step, every party broadcasts $\mathbf x_i\cdot \mathbf w_t$, where $\mathbf x_i$ is the $i$th sample in the training set and $\mathbf w_t$ is their weight at time $t$.

Suppose there are $N$ samples in the training set $\mathbf x_i\in\mathbb R^m$, and we record $T$ training iterations of $\mathbf x_i\cdot\mathbf w_t$ for $i\in[N]$ and $t\in[T]$.
Then we obtain the following matrix of inner products
\begin{equation}
    A=
    \begin{bmatrix}
        \mathbf x_1\cdot\mathbf w_1 & \cdots & \mathbf x_1\cdot\mathbf w_T\\
        \mathbf x_2\cdot\mathbf w_1 & \cdots & \mathbf x_2\cdot\mathbf w_T \\
        \vdots & \ddots & \vdots\\
        \mathbf x_N\cdot\mathbf w_1 & \cdots & \mathbf x_N\cdot\mathbf w_T
    \end{bmatrix}
    =\begin{bmatrix}
        \mathbf x_1^\intercal\\
        \mathbf x_2^\intercal\\
        \vdots\\
        \mathbf x_N^\intercal\\
    \end{bmatrix}
    \begin{bmatrix}
        \mathbf w_1&
        \mathbf w_2&
        \cdots&
        \mathbf w_T
    \end{bmatrix}
    =\mathbf X\mathbf W,
\end{equation}
where $\mathbf X\in\mathbb R^{N\times m}$ is the data matrix and $\mathbf W\in\mathbb R^{m\times T}$ is the weight matrix (each column is from a different gradient descent iteration).
This brings up the question if it is possible to recover $\mathbf X$ and $\mathbf W$, up to some linear transformation?
Such a protocol would effectively leak the training data, defeating the purpose of federated gradient descent.

Pattern matching the simplest decomposition technique we know, let's apply SVD to $A=U\Sigma V^\intercal$, where $U\in\mathbb R^{N\times r}$, $\Sigma\in\mathbb R^{r\times r}$, and $V\in\mathbb R^{T\times r}$.
Knowing $A$ has rank at most $m$, all but at most $m$ singular values in $\Sigma$ will be effectively zero, thus we can drop all columns of $U$ and $V$ after this point.
By inspection, $U$ looks similar to $\mathbf X$ and $V$ to $\mathbf W$, up to some feature selection and scaling in $\Sigma$.
\uline{Simply taking SVD is not sufficient to convincingly recover the features, as we show later, but it can act as a signal for how much information we have collected on the input data.}
\textit{Due to time constraints, we were not able to successfully implement a complete gradient inversion attack from first principles.}

\subsubsection{SVD Experiments}
To investigate the effectiveness of such an attack, we restrict our view to uncorrelated features with fixing all parameters but selection of gradient descent iterations.
The main question we want to answer is \uline{how does the SVD behave when varying how iterations are collected?}

We randomly generate 1000 data points with $\mathbf x\in\mathbb R^{100}$ and split the features among 5 parties (each holding 20 entries of $\mathbf x$ per case).
The function we want to predict is of the form $f(\mathbf x)=\mathbf w\cdot\mathbf x+\mathcal N(0,0.01)$, where $\mathbf w$ is nonzero for half of it's entries (randomly pick half of the features to matter, and other half to be irrelevant).

Running 100000 iterations of federated gradient descent with $\eta=1/10000$, we obtain training error of approximately $0.0009679$.


Recording the inner products $\mathbf x_i\cdot w_t$ every 100 iterations in $A$ from earlier, we perform SVD and obtain singular values that look like the following
\begin{verbatim}
[2.46275299e+04 1.09897010e+03 4.37783576e+02 1.53447976e+02
 6.93439793e+01 5.09262877e+01 1.08678067e+01 5.87973330e+00
 2.87236217e+00 1.36803940e+00 6.79608095e-01 4.18334821e-01
 1.54991701e-01 8.56768728e-02 3.38512748e-02 2.03707584e-02
 6.41886637e-03 2.42541993e-03 1.21015032e-03 1.02573618e-03
 1.12581855e-11 1.01822494e-11 9.73596307e-12 8.90413291e-12
 7.37457279e-12 7.14802986e-12 5.74554355e-12 4.78937451e-12
 4.53135626e-12 4.09240952e-12 3.95514437e-12 3.78223281e-12
 3.64570819e-12 3.26467281e-12 3.02962414e-12 2.79186226e-12
 2.78343760e-12 2.47092343e-12 2.39865680e-12 2.37340873e-12
 2.06942979e-12 1.77723191e-12 1.77723191e-12 1.77723191e-12
 1.77723191e-12 1.77723191e-12 1.77723191e-12 1.77723191e-12
 1.77723191e-12 1.77723191e-12 1.77723191e-12 1.77723191e-12
 1.77723191e-12 1.77723191e-12 1.77723191e-12 1.77723191e-12
 ...
\end{verbatim}

Observe that only the first $20$ singular values are actually significant, indicating we have some signal on the 20 columns of the data matrix $\mathbf X$.
Varying the frequency at which we record gradients, it is clear that the number of iterations is directly correlated with dimension of the reconstructed space.

\begin{figure}[h]
    \center
    \includegraphics[width=250px]{gia_rank}
    \caption{Plot of rank against interval for all 5 parties. Rank defined as number of singular values greater than 1e-10. Gradient is recorded every $k$ iterations, where $k=\mathrm{Interval}$.}
\end{figure}

Conjecturing that the last row of $\Sigma V^\intercal$ gives the final weights of the gradient descent iteration, we attempt to reconstruct $\mathcal Y$ from the SVD.
Unfortunately, this gives quite terrible loss, so we need something more advanced to recover usefule features.
We note that averaging over all the rows of $\Sigma V^\intercal$ does not help.

\begin{verbatim}
loop
    ...
    recovered_features.append(U_r)
    recovered_weights.append(np.diag(S_r) @ V_r.T)[:, -1]
Y_hat = sum(recovered_features[p] @ recovered_weights[p] for p in range(parties))
mean_squared_error(Y_hat, Y_atk)
> 2650.493480699639
\end{verbatim}

\subsection{Federated Averaging}
To combat this attack, we can have each party train a model train offline (with a limited feature set), then combine them at inference time.
Thus no information is passed between the models, apart from their best guess at what $\mathbf Y$ is, and the gradient inversion attack no longer works.
We effectively treat the output of each model as some feature and run another layer of regression.
This technique is noticeably worse, but still better than simply guessing randomly
\begin{verbatim}
>>> Ridge regression
Train Error: 8.745205646168433e-05
Test Error: 0.00011756193048215462
>>> Federated averaging
Train MSE: 69.42138779044436
Test MSE: 86.31839127385526
Zero: 1317.2347367455047
\end{verbatim}

Now we consider \uline{how does the error scale as we increase the data each party receives}? (Allowing for some redundancy, where multiple clients receive the same feature).
Currently, we split 100 features equally among 5 parties, each receiving 20 of them.
From the graph, we see that averaging results performs significantly better than simply taking the best party.
If we increase the multiplier of features that each party receives, we see a linear trend in the error.

\begin{figure}[h]
    \center
    \includegraphics[width=200px]{fgd_avg}
    \caption{Test and train MSE for averaged and best party against per-party feature arity multplier}
\end{figure}

\bibliographystyle{plain}
\bibliography{report.bib}

\end{document}